export class User {
	_id: string;
	username: string;
	password: string;
	firstName: string;
	lastName: string;
	//might use this as the new mdel instead of the previous user created during the tutorial
}
